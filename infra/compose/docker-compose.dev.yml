name: ekranchik-dev

services:
  opcua-simulator:
    image: python:3.12-slim
    container_name: ekranchik-opcua-simulator
    ports:
      - "4840:4840"
    volumes:
      - ../../backend:/app
    working_dir: /app
    command: python opcua_server_simulator.py
    restart: always
